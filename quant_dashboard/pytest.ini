[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short -q -m "not integration"
markers =
    integration: API smoke tests (run separately to avoid thread conflicts)
